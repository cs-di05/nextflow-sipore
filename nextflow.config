conda.enabled = true

// Script parameters

params {
    raw_reads_folder    = "$baseDir/data/"
    demulti_folder      = "$baseDir/demultiplexed_reads/"
    trim_folder         = "$baseDir/trimmed_reads/"
    help                = false
}